from tkinter import *
# add window by creating object of tkinter class
root = Tk()
# create labels
label1 = Label(root, text="hello Sandeep!")
# add labels to the window
label1.pack()
# closes only when cross button is clicked
root.mainloop()
